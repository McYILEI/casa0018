name: sylapp1
description: "Pull-Up Tracker — Flutter App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.3.3
  path: ^1.9.0
  fl_chart: ^0.68.0
  camera: ^0.10.5+9
  provider: ^6.1.1
  intl: ^0.19.0
  google_mlkit_pose_detection: ^0.12.0
  geolocator: ^13.0.0
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
